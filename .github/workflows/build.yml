name: Build postmarketOS for Samsung Galaxy A52 (samsung-a52q)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies & pmbootstrap
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip python3-setuptools git openssl
          pip3 install --user --break-system-packages pmbootstrap || pip3 install --user pmbootstrap
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Setup pmaports with samsung-a52q restored packages
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          git clone --depth 1 https://gitlab.com/postmarketOS/pmaports.git pmaports
          cp -r restored_packages/device-samsung-a52q pmaports/device/testing/
          cp -r restored_packages/firmware-samsung-a52q pmaports/device/testing/
          cp -r restored_packages/alsa-ucm-conf-samsung-sm7125 pmaports/device/testing/

      - name: Initialize pmbootstrap non-interactively
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          pmbootstrap --config-version 3 init --non-interactive --device=samsung-a52q --ui=phosh --aports="$(pwd)/pmaports" || pmbootstrap init --non-interactive --device=samsung-a52q --ui=phosh --aports="$(pwd)/pmaports"

      - name: Build device package & kernel
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          pmbootstrap build device-samsung-a52q --force

      - name: Build postmarketOS rootfs image
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          pmbootstrap --non-interactive install --password=1234

      - name: Export build artifacts
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          mkdir -p output
          pmbootstrap export "$(pwd)/output"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: postmarketOS-samsung-a52q
          path: output/*
